{
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.4",
    "tailwindcss": "^3.0.1"
  },
  "scripts": {
    "build": "tailwindcss -i style.css -o app/css/tailwind.css",
    "watch": "tailwindcss -i style.css -o app/css/tailwind.css --minify --watch",
    "minify": "tailwindcss -i style.css -o app/css/tailwind.css --minify"
  }
}
